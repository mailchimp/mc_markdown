# Use

In your Gemfile:

```ruby
gem "mc-markdown", :git => 'git@github.com:mailchimp/mc-markdown.git'
```


# Installation

```bash
$ bundle install
# => install required gems
```

# Testing

```bash
$ rspec
# => runs the test suite
```